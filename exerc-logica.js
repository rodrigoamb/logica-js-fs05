// 1. Crie duas variáveis, nome e idade, atribua valores e mostre-os no console.log() como a seguinte frase:
// “Meu nome é NOME e tenho IDADE anos.”

// ⸻

// 2. Declare uma variável x como número e uma y como string representando um número. Use console.log() para mostrar o resultado da soma de x + y. O que aconteceu?

// ⸻

// 3. Crie um objeto chamado livro com propriedades: titulo, autor e ano. Mostre cada uma das propriedades usando console.log().

// ⸻

// 4. Crie um array com 3 nomes de frutas e use console.log() para exibir o segundo item da lista.

// ⸻

// 5. Declare uma função nomeada chamada apresentar que receba dois parâmetros: nome e profissao. A função deve exibir uma frase com os dois no console.log().

// ⸻

// 6. Use uma arrow function para calcular o dobro de um número e mostrar no console.log() o resultado de passar 8 como argumento.

// ⸻

// 7. Crie uma variável estaChovendo e atribua o valor true. Mostre o valor dela com console.log().

// ⸻

// 8. Declare um objeto chamado carro com as propriedades marca, modelo e ano. Use console.log() para exibir o valor da propriedade modelo.

// ⸻

// 9. Crie uma função anônima atribuída a uma constante chamada mostrarMensagem que recebe um parâmetro e exibe esse valor no console.log().

// ⸻

// 10. Crie uma variável de escopo global chamada mensagem. Dentro de uma função, declare uma variável de mesmo nome com outro valor e exiba no console.log() os dois valores, mostrando a diferença de escopo.

// ⸻

// 11. Crie uma função nomeada que receba um array de 3 números e retorne a soma dos valores. Mostre o resultado com console.log().

// ⸻

// 12. Crie um objeto usuario com nome, idade e ativo. Depois, use console.log() para exibir se o usuário está ativo, usando o valor booleano.

// ⸻

// 13. Crie três variáveis com números e use operadores matemáticos para exibir a média entre eles com console.log().

// ⸻

// 14. Crie um array chamado bandeiras com 4 strings. Crie uma função arrow que receba o array como argumento e retorne o primeiro e o último elemento concatenados. Mostre no console.log().

// ⸻

// 15. Crie duas variáveis booleanas: temSol e fimDeSemana. Use operadores lógicos para exibir se o dia está “perfeito para praia”, apenas com console.log().

// ⸻

// 16. Crie uma variável preco com valor 10. Crie uma função anônima que recebe esse valor e retorna o valor com 20% de desconto. Mostre o resultado.

// ⸻

// 17. Crie um objeto filme com titulo, genero e nota. Mostre com console.log() se a nota é maior que 7 (usando operador relacional).

// ⸻

// 18. Crie uma variável global mensagem. Dentro de uma função arrow, declare uma variável com o mesmo nome e outro valor. Use console.log() para mostrar como o escopo de bloco interfere no valor exibido.

// ⸻

// 19. Crie um array com 5 números. Crie uma função nomeada que receba esse array e retorne a multiplicação do primeiro com o último elemento.

// ⸻

// 20. Crie um objeto pessoa e passe-o como parâmetro para uma função anônima que imprime a idade dela. Modifique o objeto fora da função e chame a função novamente. O que acontece?

// ⸻

// 21. Crie uma função nomeada chamada compararObjetos que receba dois objetos com propriedades nome e idade. Use operadores relacionais para verificar se a idade é a mesma. Mostre o resultado com console.log().

// ⸻

// 22. Crie uma arrow function que receba um array de números e retorne o valor booleano true se o primeiro for maior que o último, usando apenas operadores relacionais e lógicos.

// ⸻

// 23. Crie uma função anônima que receba dois números como parâmetro e retorne uma string no formato: "O número X é maior que Y" ou "X é menor que Y", dependendo da comparação.

// ⸻

// 24. Crie um objeto produto com nome, preco, disponivel. Crie uma arrow function que receba esse objeto e retorne uma mensagem dependendo do valor booleano da propriedade disponivel.

// ⸻

// 25. Crie duas funções: uma nomeada soma e uma arrow multiplica, ambas recebendo dois números. Faça console.log() dos resultados em escopos diferentes para mostrar que as funções podem ser acessadas de qualquer parte do código.

// ⸻

// 26. Crie um array com 4 objetos representando pessoas com nome e idade. Crie uma função que receba esse array e exiba a idade média delas no console.log().

// ⸻

// 27. Crie uma função anônima que receba dois parâmetros booleanos e retorne o resultado lógico AND entre eles. Use console.log() com diferentes combinações.

// ⸻

// 28. Crie duas variáveis numéricas no escopo global. Crie uma função nomeada que use uma delas e dentro da função declare outra com o mesmo nome. Mostre os valores antes e depois no console.log().

// ⸻

// 29. Crie uma função nomeada chamada verificaParidade que receba um número e retorne true se o número for par, usando o operador %. Mostre o resultado com console.log().

// ⸻

// 30. Crie um array de números. Crie uma função arrow que receba esse array e retorne um novo array com o dobro de cada valor, apenas usando operadores matemáticos e console.log().
